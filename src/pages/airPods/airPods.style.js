import styled from "styled-components";


export const AirPodsWrapper = styled.div`
    background-color: var(--color-screen-glow);
    padding-top: 44px;
    overflow: hidden;

    & header {
        /* position: absolute; */
    }
`

export const AirPodsContainer = styled.div`
    max-width: 1520px;
    width: 100%;
    padding: 0 20px;
    margin: 0 auto;
`